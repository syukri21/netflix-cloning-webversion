export const styles = (theme) => ({
	gradient: {
		background: `linear-gradient(to left, ${theme.palette.secondary.main}44 20%, ${theme.palette.primary
			.main}, ${theme.palette.primary.main})`,
		zIndex: 50,
		position: 'absolute',
		top: 0,
		left: 0,
		right: 0,
		bottom: 0
	}
});
